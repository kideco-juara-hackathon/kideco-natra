[project]
name = "kideco-backend"
version = "0.1.0"
description = "FastAPI backend for Kideco Logistics Control"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.111.0",
  "uvicorn[standard]>=0.30.0"
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
