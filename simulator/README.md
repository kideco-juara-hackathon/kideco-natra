# Simulator

HTTP telemetry simulator for MVP demos.

## Planned Run Command

```bash
python telemetry_simulator.py --scenario normal
```

By default, telemetry events are sent to:

```text
http://localhost:8000/api/telemetry
```
