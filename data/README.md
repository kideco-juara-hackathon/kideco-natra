# Data

This folder stores seed data and sanitized datasets.

## Policy

- `raw`: local-only raw datasets. Do not commit internal data without permission.
- `processed`: derived datasets for ML experiments.
- `seeds`: small sanitized JSON files used by backend and frontend demos.
